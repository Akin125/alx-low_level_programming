#include <stdio.h>

/**
 * main - prints numbers from 0 - 9
 *
 * Description - prints in base 10 starting from 0
 *
 * Return : the return value of main here 
 */

int main(void)
{
int num;
for (num = 0; num < 10; num++)
printf("%d", num);
printf("\n");
return (0);
}

